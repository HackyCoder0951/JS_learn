/* 
Create a function called "beautifyString" 

which accept only one argument.

and convert that string into desired format.
*/
/*
:-> The first character of every word in a string should be capital case and remaining in lower case.
:-> Every word of the string should be separated by "-" (dash) symbol.
:-> You must need to remove every numbers from the string.
:-> If string contain only a single word then do not add "-" (dash) symbol.
*/
/* Test Cases */
/* Input */
    // beautifyString("hello world")
    // beautifyString("This is soMe text")
    // beautifyString("the MAN 90 is lost HIS power")
    // beautifyString("hi")

/* Output */
    // Hello-World
    // This-Is-Some-Text
    // The-Man-Is-Lost-His-Power
    // Hi
    