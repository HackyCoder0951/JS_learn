# JavaScript Strings

## Introduction

- The JavaScript string is an object that represents a sequence of characters.

### There are 2 ways to create string in JavaScript

1. By string literal  
2. By string object (using new keyword)

---

# 1) By String Literal

- The string literal is created using double quotes.

## Syntax

```js
var stringname = "string value";
```

---

# 2) By String Object (Using New Keyword)

- The syntax of creating string object using new keyword is given below:

## Syntax

```js
var stringname = new String("string literal");
```

- Here, `new` keyword is used to create instance of string.

---

# Template Literals

- The backticks (` `) are used to define a template literal.
- Template literals allow embedded expressions.

## Example

```js
let name = "Jignesh";

console.log(`Hello ${name}`);
```

---

# JavaScript String Methods

| Methods | Description |
|---|---|
| `charAt()` | It provides the char value present at the specified index. |
| `charCodeAt()` | It provides the Unicode value of a character present at the specified index. |
| `concat()` | It provides a combination of two or more strings. |
| `indexOf()` | It provides the position of a char value present in the given string. |
| `lastIndexOf()` | It provides the position of a char value present in the given string by searching a character from the last position. |
| `search()` | It searches a specified regular expression in a given string and returns its position if a match occurs. |
| `match()` | It searches a specified regular expression in a given string and returns that regular expression if a match occurs. |
| `replace()` | It replaces a given string with the specified replacement. |
| `substr()` | It is used to fetch the part of the given string on the basis of the specified starting position and length. |
| `substring()` | It is used to fetch the part of the given string on the basis of the specified index. |
| `slice()` | It is used to fetch the part of the given string. It allows us to assign positive as well negative index. |
| `toLowerCase()` | It converts the given string into lowercase letter. |
| `toLocaleLowerCase()` | It converts the given string into lowercase letter on the basis of host’s current locale. |
| `toUpperCase()` | It converts the given string into uppercase letter. |
| `toLocaleUpperCase()` | It converts the given string into uppercase letter on the basis of host’s current locale. |
| `toString()` | It provides a string representing the particular object. |
| `valueOf()` | It provides the primitive value of string object. |
| `split()` | It splits a string into substring array, then returns that newly created array. |
| `trim()` | It trims the white space from the left and right side of the string. |

---

# 01 - JS String `at(index)` Method

## Description

- The `at()` method returns an indexed character from a string.
- The `at()` method returns the same as `[]`.

## Syntax

```js
string.at(index)
```

## Example

```js
{
    let text = "W3Schools",
    character = text.at(0);

    console.log("01 - JS String 'at(index)' Method = ", character);
}
```

---

# 02 - JS String `charAt(index)` Method

## Description

- The `charAt()` method returns the character at a specified index (position) in a string.
- The index of the first character is `0`, the second is `1`, and so on.

## Syntax

```js
string.charAt(index)
```

## Example

```js
{
    let text = "HELLO WORLD";

    let letter = text.charAt(0);

    console.log("02 - JS String 'charAt(index)' Method = ", letter);
}
```

---

# 03 - JS String `charCodeAt(index)` Method

## Description

- The `charCodeAt()` method returns an integer between `0` and `65535`.
- It represents the UTF-16 code unit at the given index.

## Syntax

```js
string.charCodeAt(index)
```

## Example

```js
{
    let text = "HELLO WORLD";

    let code = text.charCodeAt(0);

    console.log("03 - JS String 'charCodeAt(index)' Method = ", code);
}
```

---

# 04 - JS String `codePointAt(index)` Method

## Description

- The `codePointAt()` method returns the Unicode code point value of the character starting at the given index.

## Syntax

```js
string.codePointAt(index)
```

## Example

```js
{
    let text = "HELLO WORLD";

    let code = text.codePointAt(0);

    console.log("04 - JS String 'codePointAt(index)' Method = ", code);
}
```

---

# 05 - JS String `concat(string)` Method

## Description

- The `concat()` method joins two or more strings.
- The `concat()` method does not change the existing strings.
- The `concat()` method returns a new string.

## Syntax

```js
string.concat(string1, string2, ...stringX)
```

## Example

```js
{
    let text1 = "sea";
    let text2 = "food";

    let result = text1.concat(text2);

    console.log("05 - JS String 'text1.concat(text2);' Method = ", result);
}
```

---

# 06 - JS String `constructor` Method

## Description

- The `constructor` property returns the function that created the String prototype.

### For JavaScript strings the constructor returns:

```js
function String() { [native code] }
```

## Syntax

```js
string.constructor
```

## Example

```js
{
    let message = "hello world";

    let text = message.constructor;

    console.log("06 - JS String 'string.constructor' Method = ", text);
}
```

---

# 07 - JS String `endsWith(searchValue, length)` Method

## Description

- The `endsWith()` method returns `true` if a string ends with a specified string.
- Otherwise it returns `false`.
- The `endsWith()` method is case sensitive.

## Syntax

```js
string.endsWith(searchValue, length)
```

## Example

```js
{
    let text = "Hello World";

    let result = text.endsWith("World");

    console.log("07 - JS String 'endsWith()' Method = ", result);
}
```

---

# 08 - JS String `fromCharCode()` Method

## Description

- The `String.fromCharCode()` method converts Unicode values to characters.
- It is a static method of the `String` object.
- The syntax is always `String.fromCharCode()`.

## Syntax

```js
String.fromCharCode(n1, n2, ..., nX)
```

## Example

```js
{
    let char = String.fromCharCode(72, 69, 76, 76, 79);

    console.log(
        "08 - JS String 'String.fromCharCode(72, 69, 76, 76, 79)' Method = ",
        char
    );
}
```

---

# 09 - JS String `fromCodePoint()` Method

## Description

- `fromCodePoint()` is a static method of `String`.
- Unicode code points range from `0` to `1114111 (0x10FFFF)`.

## Syntax

```js
String.fromCodePoint(n1, n2, ...., nX)
```

## Example

```js
{
    let result = String.fromCodePoint(9731, 9733, 9842, 0x2f804);

    console.log(
        "09 - JS String String.fromCodePoint(9731, 9733, 9842, 0x2f804) Method = ",
        result
    );
}
```

---

# 10 - JS String `includes()` Method

## Description

- The `includes()` method returns `true` if a string contains a specified string.
- Otherwise it returns `false`.
- The `includes()` method is case sensitive.

## Syntax

```js
string.includes(searchvalue, start)
```

## Example

```js
{
    let text = "hello world, welcome to the universe.";

    let result = text.includes("world");

    console.log("10 - JS String 'includes()' Method = ", result);
}
```

---

# 11 - JS String `indexOf()` Method

## Description

- The `indexOf()` method returns the position of the first occurrence of a value in a string.
- It returns `-1` if the value is not found.
- The method is case sensitive.

## Syntax

```js
string.indexOf(searchValue, Start)
```

## Example

```js
{
    const paragraph = "I think Ruth's dog is cuter than your dog!";

    const searchTerm = "dog";

    const indexOfFirst = paragraph.indexOf(searchTerm);

    console.log(
        `11 - The index of the first "${searchTerm}" is ${indexOfFirst}`
    );

    console.log(
        `11.1 - The index of the second "${searchTerm}" is ${
            paragraph.indexOf(searchTerm, indexOfFirst + 1)
        }`
    );
}
```

---

# Difference Between `indexOf()` and `search()`

| `indexOf()` | `search()` |
|---|---|
| Cannot search using regular expressions | Can search using regular expressions |
| Supports start position | Does not support start position |

---

# 12 - JS String `isWellFormed()` Method

## Description

- `isWellFormed()` checks whether a string is well-formed.
- It detects invalid surrogate pairs.

## Syntax

```js
string.isWellFormed()
```

## Example

```js
{
    const strings = [
        "ab\uD800",
        "ab\uD800c",
        "\uDFFFab",
        "c\uDFFFab",
        "abc",
        "ab\uD83D\uDE04c",
    ];

    for (const str of strings) {
        console.log(
            "12 - JS String str.isWellFormed() Method = ",
            str.isWellFormed()
        );
    }
}
```

---

# 13 - JS String `lastIndexOf()` Method

## Description

- The `lastIndexOf()` method returns the last occurrence of a specified value.
- It searches from end to beginning.
- It returns the index from the beginning.

## Syntax

```js
string.lastIndexOf(searchValue, Start)
```

## Example

```js
{
    const paragraph = "I think Ruth's dog is cuter than your dog!";

    const searchTerm = "dog";

    console.log(
        `13 - Index of the last "${searchTerm}" is ${
            paragraph.lastIndexOf(searchTerm)
        }`
    );
}
```

---

# 14 - JS String `length` Property

## Description

- The `length` property returns the number of characters in a string.

## Syntax

```js
string.length
```

## Example

```js
{
    const str = "Life, the universe and everything. Answer:";

    console.log(
        `14 - JS String 'length' Method = ${str.length}`
    );
}
```

---

# 15 - JS String `localeCompare()` Method

## Description

- The `localeCompare()` method compares two strings in the current locale.
- Returns:
  - `-1` → before
  - `1` → after
  - `0` → equal

## Syntax

```js
localeCompare(compareString)
```

## Example

```js
{
    let text1 = "ab";
    let text2 = "bf";

    let result = text1.localeCompare(text2);

    console.log(
        "15 - JS String 'text1.localeCompare(text2)' Method = ",
        result
    );
}
```

---

# 16 - JS String `match(regexp)` Method

## Description

- The `match()` method matches a string against a regular expression.
- It returns an array with the matches.
- It returns `null` if no match is found.

## Syntax

```js
string.match(regexp)
```

## Example

```js
{
    const str = "For more information, see Chapter 3.4.5.1";

    const re = /see (chapter \d+(\.\d)*)/i;

    const found = str.match(re);

    console.log("16 - JS String 'str.match(re)' Method = ", found);
}
```

---

# Difference Between `match()` and `search()`

| `match()` | `search()` |
|---|---|
| Returns array of matches | Returns position of first match |
| Returns matched values | Returns index value |

---

# 17 - JS String `matchAll()` Method

## Description

- The `matchAll()` method returns an iterator.
- It returns all matches including capturing groups.

## Syntax

```js
string.matchAll(regexp)
```

## Example

```js
{
    const regexp = /t(e)(st(\d?))/g;

    const str = "test1test2";

    const array = [...str.matchAll(regexp)];

    console.log(
        "17 - JS String '[...str.matchAll(regexp)]' Method = ",
        array[0]
    );

    console.log(
        "17.1 - JS String '[...str.matchAll(regexp)]' Method = ",
        array[1]
    );
}
```

---

# 18 - JS String `normalize()` Method

## Description

- The `normalize()` method returns the Unicode Normalization Form of a string.

## Syntax

```js
normalize()
normalize(form)
```

### Forms

- `"NFC"`
- `"NFD"`
- `"NFKC"`
- `"NFKD"`

## Example

```js
{
    const name1 = "\u0041\u006d\u00e9\u006c\u0069\u0065";

    const name2 = "\u0041\u006d\u0065\u0301\u006c\u0069\u0065";

    console.log(
        "18 - JS String 'normalize()' Method = ",
        `${name1}, ${name2}`
    );

    console.log(
        "18.1 - name1 === name2 = ",
        name1 === name2
    );

    console.log(
        "18.2 - name1.length === name2.length = ",
        name1.length === name2.length
    );

    const name1NFC = name1.normalize("NFC");

    const name2NFC = name2.normalize("NFC");

    console.log(
        "18.3 - JS String 'normalize()' Method = ",
        `${name1NFC}, ${name2NFC}`
    );

    console.log(
        "18.4 - name1NFC === name2NFC = ",
        name1NFC === name2NFC
    );

    console.log(
        "18.5 - name1NFC.length === name2NFC.length = ",
        name1NFC.length === name2NFC.length
    );
}
```

---

# 19 - JS String `padEnd()` Method

## Description

- The `padEnd()` method pads a string at the end.
- It pads the string until it reaches a given length.

## Syntax

```js
string.padEnd(length, string)
```

## Example

```js
{
    let text = "Hello World ";

    text = text.padEnd(25, " hello world");

    console.log(
        "19 - JS String 'padEnd()' Method = ",
        text
    );
}
```

---

# 20 - JS String `padStart()` Method

## Description

- The `padStart()` method pads a string from the start.
- It pads until the string reaches the given length.

## Syntax

```js
string.padStart(length, string)
```

## Example

```js
{
    let text = "5";

    text = text.padStart(4, "0");

    console.log(
        "20 - JS String 'padStart()' Method = ",
        text
    );
}
```

---

# 21 - JS String `prototype` Property

## Description

- The `prototype` property allows adding new methods and properties to objects.

## Syntax

```js
String.prototype.name = value
```

## Example

```js
{
    String.prototype.greet = function () {
        return "Hello " + this;
    };

    let text = "Jignesh";

    console.log(
        "21 - JS String 'prototype' Method = ",
        text.greet()
    );
}
```

---

# 22 - JS String `raw()` Method

## Description

- `String.raw()` returns the raw string form of template literals.

## Syntax

```js
String.raw(strings, substitutions)
```

## Example

```js
{
    let text = String.raw`Hello\nWorld`;

    console.log(
        "22 - JS String 'raw()' Method = ",
        text
    );
}
```

---

# 23 - JS String `repeat()` Method

## Description

- The `repeat()` method returns a new string repeated specified number of times.

## Syntax

```js
string.repeat(count)
```

## Example

```js
{
    let text = "Hello ";

    console.log(
        "23 - JS String 'repeat()' Method = ",
        text.repeat(3)
    );
}
```

---

# 24 - JS String `replace()` Method

## Description

- The `replace()` method replaces a specified value with another value.

## Syntax

```js
string.replace(searchValue, newValue)
```

## Example

```js
{
    let text = "I love Java";

    let result = text.replace("Java", "JavaScript");

    console.log(
        "24 - JS String 'replace()' Method = ",
        result
    );
}
```

---

# 25 - JS String `replaceAll()` Method

## Description

- The `replaceAll()` method replaces all matches of a string.

## Syntax

```js
string.replaceAll(searchValue, newValue)
```

## Example

```js
{
    let text = "cat dog cat";

    let result = text.replaceAll("cat", "lion");

    console.log(
        "25 - JS String 'replaceAll()' Method = ",
        result
    );
}
```

---

# 26 - JS String `search()` Method

## Description

- The `search()` method searches a string for a match against a regular expression.
- It returns the index of the first match.
- Returns `-1` if no match is found.

## Syntax

```js
string.search(regexp)
```

## Example

```js
{
    let text = "Learn JavaScript";

    let result = text.search("JavaScript");

    console.log(
        "26 - JS String 'search()' Method = ",
        result
    );
}
```

---

# 27 - JS String `slice()` Method

## Description

- The `slice()` method extracts a part of a string.
- It returns the extracted part in a new string.
- It supports negative indexes.

## Syntax

```js
string.slice(start, end)
```

## Example

```js
{
    let text = "JavaScript";

    console.log(
        "27 - JS String 'slice()' Method = ",
        text.slice(0, 4)
    );

    console.log(
        "27.1 - JS String 'slice()' Method = ",
        text.slice(-6)
    );
}
```

---

# 28 - JS String `split()` Method

## Description

- The `split()` method splits a string into an array of substrings.

## Syntax

```js
string.split(separator, limit)
```

## Example

```js
{
    let text = "apple,mango,banana";

    let result = text.split(",");

    console.log(
        "28 - JS String 'split()' Method = ",
        result
    );
}
```

---

# 29 - JS String `startsWith()` Method

## Description

- The `startsWith()` method checks whether a string begins with specified characters.
- Returns `true` or `false`.

## Syntax

```js
string.startsWith(searchValue, start)
```

## Example

```js
{
    let text = "JavaScript";

    let result = text.startsWith("Java");

    console.log(
        "29 - JS String 'startsWith()' Method = ",
        result
    );
}
```

---

# 30 - JS String `substr()` Method

## Description

- The `substr()` method extracts a part of a string.
- It takes starting index and length.

> Note: `substr()` is deprecated.

## Syntax

```js
string.substr(start, length)
```

## Example

```js
{
    let text = "JavaScript";

    let result = text.substr(0, 4);

    console.log(
        "30 - JS String 'substr()' Method = ",
        result
    );
}
```

---

# 31 - JS String `substring()` Method

## Description

- The `substring()` method extracts characters between two indexes.
- Negative values are treated as `0`.

## Syntax

```js
string.substring(start, end)
```

## Example

```js
{
    let text = "JavaScript";

    let result = text.substring(0, 4);

    console.log(
        "31 - JS String 'substring()' Method = ",
        result
    );
}
```

---

# 32 - JS String `toLocaleLowerCase()` Method

## Description

- The `toLocaleLowerCase()` method converts a string to lowercase according to locale.

## Syntax

```js
string.toLocaleLowerCase()
```

## Example

```js
{
    let text = "HELLO WORLD";

    console.log(
        "32 - JS String 'toLocaleLowerCase()' Method = ",
        text.toLocaleLowerCase()
    );
}
```

---

# 33 - JS String `toLocaleUpperCase()` Method

## Description

- The `toLocaleUpperCase()` method converts a string to uppercase according to locale.

## Syntax

```js
string.toLocaleUpperCase()
```

## Example

```js
{
    let text = "hello world";

    console.log(
        "33 - JS String 'toLocaleUpperCase()' Method = ",
        text.toLocaleUpperCase()
    );
}
```

---

# 34 - JS String `toLowerCase()` Method

## Description

- The `toLowerCase()` method converts a string to lowercase letters.

## Syntax

```js
string.toLowerCase()
```

## Example

```js
{
    let text = "HELLO WORLD";

    console.log(
        "34 - JS String 'toLowerCase()' Method = ",
        text.toLowerCase()
    );
}
```

---

# 35 - JS String `toString()` Method

## Description

- The `toString()` method returns a string representation of an object.

## Syntax

```js
object.toString()
```

## Example

```js
{
    let num = 100;

    console.log(
        "35 - JS String 'toString()' Method = ",
        num.toString()
    );
}
```

---

# 36 - JS String `toUpperCase()` Method

## Description

- The `toUpperCase()` method converts a string to uppercase letters.

## Syntax

```js
string.toUpperCase()
```

## Example

```js
{
    let text = "hello world";

    console.log(
        "36 - JS String 'toUpperCase()' Method = ",
        text.toUpperCase()
    );
}
```

---

# 37 - JS String `toWellFormed()` Method

## Description

- The `toWellFormed()` method converts ill-formed strings into well-formed strings.

## Syntax

```js
string.toWellFormed()
```

## Example

```js
{
    let text = "hello";

    console.log(
        "37 - JS String 'toWellFormed()' Method = ",
        text.toWellFormed()
    );
}
```

---

# 38 - JS String `trim()` Method

## Description

- The `trim()` method removes whitespace from both sides of a string.

## Syntax

```js
string.trim()
```

## Example

```js
{
    let text = "   hello world   ";

    console.log(
        "38 - JS String 'trim()' Method = ",
        text.trim()
    );
}
```

---

# 39 - JS String `trimEnd()` Method

## Description

- The `trimEnd()` method removes whitespace from the end of a string.

## Syntax

```js
string.trimEnd()
```

## Example

```js
{
    let text = "hello world      ";

    console.log(
        "39 - JS String 'trimEnd()' Method = ",
        text.trimEnd()
    );
}
```

---

# 40 - JS String `trimStart()` Method

## Description

- The `trimStart()` method removes whitespace from the beginning of a string.

## Syntax

```js
string.trimStart()
```

## Example

```js
{
    let text = "      hello world";

    console.log(
        "40 - JS String 'trimStart()' Method = ",
        text.trimStart()
    );
}
```

---

# 41 - JS String `valueOf()` Method

## Description

- The `valueOf()` method returns the primitive value of a string object.

## Syntax

```js
string.valueOf()
```

## Example

```js
{
    let text = new String("Hello World");

    console.log(
        "41 - JS String 'valueOf()' Method = ",
        text.valueOf()
    );
}
```

---

# Additional JavaScript String Examples

## Single Quotes, Double Quotes and Backticks

```js
let single = 'single-quoted';

let double = "double-quoted";

let backticks = `backticks`;
```

---

# Template Literal Example

## Example

```js
const name = "jignesh";

const repoCount = 36;

// console.log(name + " " + repoCount);

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);
```

---

# Creating String Object

## Example

```js
const gameName = new String('Jignesh-Ja-BCA');
```

---

# Access Characters from String

## Example

```js
console.log(gameName[0]);
```

---

# Access Prototype

## Example

```js
console.log(gameName.__proto__);
```

---

# String Length

## Example

```js
console.log(gameName.length);
```

---

# Convert String to Uppercase

## Example

```js
console.log(gameName.toUpperCase());
```

---

# Find Character using `charAt()`

## Example

```js
console.log(gameName.charAt(2));
```

---

# Find Index using `indexOf()`

## Example

```js
console.log(gameName.indexOf('h'));
```

---

# Using `substring()`

## Description

- `substring()` extracts characters between indexes.

## Example

```js
const newString = gameName.substring(0, 4);

console.log(newString);
```

---

# Using `slice()`

## Description

- `slice()` can also work with negative indexes.

## Example

```js
const anotherString = gameName.slice(-8, 5);

console.log(anotherString);
```

---

# Using `trim()`

## Description

- `trim()` removes extra spaces from both sides.

## Example

```js
const newStringOne = "   jignesh    ";

console.log(newStringOne);

console.log(newStringOne.trim());
```

---

# Using `replace()`

## Description

- `replace()` replaces a specific value inside a string.

## Example

```js
const url = "https://linkedin.com/in/jignesh%20ameta";

console.log(url.replace('%20', '-'));
```

---

# Using `includes()`

## Description

- `includes()` checks whether a value exists inside a string.

## Example

```js
console.log(url.includes('abcd'));

console.log(url.includes('jign'));
```

---

# Using `split()`

## Description

- `split()` converts string into array.

## Example

```js
console.log(gameName.split('-'));
```

---

# Real World Practical Examples

---

# Example 1 - User Full Name Formatting

```js
let firstName = "Jignesh";
let lastName = "Ameta";

let fullName = firstName.concat(" ", lastName);

console.log(fullName);
```

---

# Example 2 - Email Validation

```js
let email = "jignesh@gmail.com";

console.log(email.includes("@"));
```

---

# Example 3 - Remove Extra Spaces from Input

```js
let userInput = "    hello world    ";

console.log(userInput.trim());
```

---

# Example 4 - Convert User Input to Uppercase

```js
let city = "delhi";

console.log(city.toUpperCase());
```

---

# Example 5 - Generate Username

```js
let username = "Jignesh Ameta";

console.log(username.toLowerCase().replace(" ", "_"));
```

---

# Example 6 - URL Slug Generator

```js
let title = "JavaScript String Methods";

let slug = title.toLowerCase().replaceAll(" ", "-");

console.log(slug);
```

---

# Example 7 - Count Characters

```js
let password = "abc@123";

console.log(password.length);
```

---

# Example 8 - Extract Domain from Email

```js
let email = "jignesh@gmail.com";

console.log(email.split("@")[1]);
```

---

# Example 9 - Repeat Characters

```js
console.log("*".repeat(10));
```

---

# Example 10 - Check File Extension

```js
let fileName = "photo.png";

console.log(fileName.endsWith(".png"));
```

---

# Example 11 - Capitalize First Letter

```js
let word = "javascript";

let capitalized =
    word.charAt(0).toUpperCase() + word.slice(1);

console.log(capitalized);
```

---

# Example 12 - Mask Credit Card Number

```js
let cardNumber = "1234567812345678";

let masked =
    cardNumber.slice(-4).padStart(cardNumber.length, "*");

console.log(masked);
```

---

# Example 13 - Reverse String

```js
let text = "JavaScript";

let reversed =
    text.split("").reverse().join("");

console.log(reversed);
```

---

# Example 14 - Check Palindrome

```js
let word = "madam";

let reversed =
    word.split("").reverse().join("");

console.log(word === reversed);
```

---

# Example 15 - Count Words

```js
let sentence = "I love JavaScript";

let words = sentence.split(" ");

console.log(words.length);
```

---

# Summary

- JavaScript strings are used to store and manipulate text.
- Strings can be created using:
  - String literals
  - String objects
- JavaScript provides many built-in methods for:
  - Searching
  - Extracting
  - Replacing
  - Formatting
  - Validating
  - Transforming strings

---

# Important Interview Questions on JavaScript Strings

---

# 1) What is a String in JavaScript?

## Answer

- A string is a sequence of characters used to represent text.
- Strings can be created using:
  - Single quotes `' '`
  - Double quotes `" "`
  - Backticks `` ` ` ``

## Example

```js
let name = "Jignesh";
```

---

# 2) Difference Between String Literal and String Object

| String Literal | String Object |
|---|---|
| Primitive datatype | Object datatype |
| Faster | Slower |
| Recommended | Rarely used |

## Example

```js
let str1 = "Hello";

let str2 = new String("Hello");
```

---

# 3) Difference Between `slice()`, `substring()` and `substr()`

| Method | Description |
|---|---|
| `slice()` | Supports negative indexes |
| `substring()` | Does not support negative indexes |
| `substr()` | Uses starting index and length |

## Example

```js
let text = "JavaScript";

console.log(text.slice(0, 4));

console.log(text.substring(0, 4));

console.log(text.substr(0, 4));
```

---

# 4) Difference Between `==` and `===` in Strings

## Answer

| Operator | Description |
|---|---|
| `==` | Checks value only |
| `===` | Checks value and datatype |

## Example

```js
console.log("5" == 5);

console.log("5" === 5);
```

---

# 5) What is Template Literal?

## Answer

- Template literals use backticks.
- They allow:
  - Multi-line strings
  - Variable interpolation
  - Embedded expressions

## Example

```js
let name = "Jignesh";

console.log(`Hello ${name}`);
```

---

# 6) How to Reverse a String?

## Example

```js
let text = "JavaScript";

let reversed =
    text.split("").reverse().join("");

console.log(reversed);
```

---

# 7) How to Check Palindrome String?

## Example

```js
let word = "madam";

let reversed =
    word.split("").reverse().join("");

console.log(word === reversed);
```

---

# 8) How to Count Characters in String?

## Example

```js
let text = "JavaScript";

console.log(text.length);
```

---

# 9) How to Convert String into Array?

## Example

```js
let fruits = "apple,mango,banana";

console.log(fruits.split(","));
```

---

# 10) Difference Between `indexOf()` and `includes()`

| `indexOf()` | `includes()` |
|---|---|
| Returns index | Returns boolean |
| Returns `-1` if not found | Returns `false` if not found |

## Example

```js
let text = "JavaScript";

console.log(text.indexOf("Script"));

console.log(text.includes("Script"));
```

---

# 11) How to Remove Spaces from String?

## Example

```js
let text = "   hello world   ";

console.log(text.trim());
```

---

# 12) How to Replace Text in String?

## Example

```js
let text = "I love Java";

console.log(
    text.replace("Java", "JavaScript")
);
```

---

# 13) How to Convert String to Uppercase and Lowercase?

## Example

```js
let text = "JavaScript";

console.log(text.toUpperCase());

console.log(text.toLowerCase());
```

---

# 14) How to Check String Starts or Ends With Specific Text?

## Example

```js
let fileName = "photo.png";

console.log(fileName.startsWith("photo"));

console.log(fileName.endsWith(".png"));
```

---

# 15) How to Extract Part of a String?

## Example

```js
let text = "JavaScript";

console.log(text.slice(0, 4));
```

---

# 16) How to Repeat a String?

## Example

```js
console.log("Hi ".repeat(3));
```

---

# 17) How to Find Character at Specific Index?

## Example

```js
let text = "JavaScript";

console.log(text.charAt(2));

console.log(text.at(2));
```

---

# 18) How to Convert Number into String?

## Example

```js
let num = 100;

console.log(num.toString());
```

---

# 19) How to Check String Length?

## Example

```js
let password = "abc@123";

console.log(password.length);
```

---

# 20) How to Mask Sensitive Data?

## Example

```js
let cardNumber = "1234567812345678";

let masked =
    cardNumber.slice(-4).padStart(cardNumber.length, "*");

console.log(masked);
```

---

# Commonly Used String Methods

| Method | Purpose |
|---|---|
| `length` | Count characters |
| `slice()` | Extract text |
| `substring()` | Extract text |
| `replace()` | Replace text |
| `replaceAll()` | Replace all matches |
| `split()` | Convert to array |
| `trim()` | Remove spaces |
| `includes()` | Check existence |
| `indexOf()` | Find index |
| `toUpperCase()` | Uppercase |
| `toLowerCase()` | Lowercase |

---

# Final Notes

- Strings are immutable in JavaScript.
- String methods return new strings.
- Original strings are not modified.
- String methods are heavily used in:
  - Form validation
  - Search functionality
  - Data formatting
  - URL handling
  - Text processing

---
